# 丰碑网络 —— Python 依赖
# 安装：pip install -r requirements.txt

# ─── 核心依赖 ──────────────────────────────────────────────

# HTTP API 服务
Flask>=3.0

# 加密（Ed25519 签名）
cryptography>=38.0

# 配置验证（JSON Schema）
jsonschema>=4.0.0

# YAML 配置文件解析（event_rules.yaml, tasks.yaml）
PyYAML>=6.0

# Cron 表达式解析（task_scheduler.py）
croniter>=2.0

# Kademlia DHT 节点发现
kademlia==2.2.3
rpcudp>=5.0
u-msgpack-python>=2.6

# ─── 群聊服务依赖（core/group_chat_api.py） ──────────────

# FastAPI + ASGI 服务器
fastapi>=0.100
uvicorn>=0.23

# 数据验证
pydantic>=2.0

# 向量数据库（内存模式可不安装，但建议安装）
qdrant-client>=1.7

# ─── 可选：跨实例同步 ─────────────────────────────────────

# HTTP 客户端请求
# requests>=2.31
